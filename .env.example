# Example environment variables for local development and production
# Create a file named .env.local (for local dev) or set these in Vercel
# Values prefixed with VITE_ are exposed to the frontend by Vite.
VITE_API_URL=http://localhost:5000
VITE_DATA_API_URL=https://dummyjson.com
